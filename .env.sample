# ----- Mandatory Configurations -----
PORT=8000                           # Your server port
CORS_ORIGIN=*                       # Allows all origins (adjust in production)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-name>.<db-server>.mongodb.net/<dbname>?retryWrites=true&w=majority
                                    # Replace with your actual MongoDB Atlas URI

ACCESS_TOKEN_SECRET=                # Generate a secure random string for JWT signing
ACCESS_TOKEN_EXPIRY=2h              # Access token expiry (e.g., 2h, 15m)
REFRESH_TOKEN_SECRET=               # Generate another secure random string for JWT signing
REFRESH_TOKEN_EXPIRY=10d            # Refresh token expiry (e.g., 10d, 30d)

CLOUDINARY_CLOUD_NAME=your_cloud_name     # From your Cloudinary dashboard
CLOUDINARY_API_KEY=your_api_key           # From your Cloudinary dashboard
CLOUDINARY_API_SECRET=your_api_secret     # From your Cloudinary dashboard

# ----- Optional / Notes -----
# NODE_ENV=development               # Optional: development, production, etc.
# JWT_COOKIE_EXPIRE=86400            # Optional: Cookie expiry (seconds)
# JWT_COOKIE_HTTP_ONLY=true          # Optional: Set to true to make the cookie secure
